# Copie este arquivo para `.env` e preencha os valores.
# IMPORTANTE: use os MESMOS valores do app mobile — backend e Supabase
# são compartilhados; este projeto é apenas a interface web do painel.

# Backend Express (o mesmo do app mobile)
VITE_API_URL=http://localhost:3005

# Supabase — mesmo projeto do app
VITE_SUPABASE_URL=https://daxawxyjysutewsulsgk.supabase.co
VITE_SUPABASE_ANON_KEY=cole-aqui-a-anon-key-do-supabase
